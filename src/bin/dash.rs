use dashmap::DashMap;
use std::sync::Arc;

fn main() {
    // let map = Arc::new(DashMap::new());
    // map.insert(1, "a");

    // // Create a reference to an entry
    // if let Some(entry) = map.get(&1) {
    //     // Try to insert a new value while holding a reference to an entry
    //     map.insert(2, "b");
    // }
}